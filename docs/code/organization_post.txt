
$search = $client->searchOrganization('organization_wave');
$organization = $search[0];
$organization->setTenantDesc('Do the wave');

try {
    $client->updateOrganization($organization);
} catch (Exception $e) {
    printf('Unable to modify organization: %s', $e->getMessage());
}